---
sudo: required

language: node_js

node_js:
  - "7"

services:
  - docker

before_install:
  - ng install -g @angular/cli
  - npm install
  - npm install --only=dev

script:
  - ng install --prod
