image: 7.8.0
before_script:
  - apt-get update -qq && apt-get install -y -qq build-essential
  - npm install -g @angular/cli

build:
  script:
    npm install
    ng bundle
    
test:
  script:
    npm test
    npm e2e