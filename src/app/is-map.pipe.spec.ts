/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IsMapPipe } from './is-map.pipe';

describe('IsMapPipe', () => {
  it('create an instance', () => {
    let pipe = new IsMapPipe();
    expect(pipe).toBeTruthy();
  });
});
